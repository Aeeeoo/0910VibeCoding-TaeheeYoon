# DESIGN.md

## 이 서비스는 뭘 하나

윤태희를 소개하는 한 페이지짜리 웹사이트다. 스크롤 한 번이면 끝나는 분량에
소개 글, 관심사, 교내·교외 활동, 연락 링크를 담았다.

화면 전체가 **80~90년대 오락실 게임 화면처럼 생겼다.** 실제로 조작할 수 있는
게임은 아니고, 아케이드 화면을 흉내 낸 정적 페이지다.

## 화면에 있는 것들

굵은 파란 테두리를 두른 게임 캐비닛 하나 안에 전부 들어 있다. 위에서 아래로
한 줄씩 쌓이고, 화면 전체에 옅은 CRT 스캔라인이 깔린다.

### 상단 표시줄 (장식)

오락실 점수판 그대로다. `1UP` / `HIGH SCORE`(깜빡임) / `CREDIT` 세 칸.
숫자는 분위기용이라 실제 의미는 없다.

### 타이틀 화면

- `★ ONE PAGE ARCADE ★` 작은 문구
- **이름** — 로마자 표기가 가장 큰 글자로 두 줄, 노란색에 두 겹 그림자
- 그 아래 한글 이름(글자 사이를 넓게), 한 줄 소개(하늘색)
- **팩맨과 유령 셋** — 팩맨은 입을 벌렸다 닫고, 유령들은 위아래로 흔들리며
  옆으로 조금씩 전진한다
- **REDIRECT TO PORTFOLIO** — 노란 테두리를 두른 큼직한 버튼. 누르면 노션
  포트폴리오가 새 탭에서 열린다. 이 페이지에서 가장 눈에 띄는 누를 거리다
- `▼ SCROLL TO START ▼` 깜빡이는 안내

### 스테이지 네 개

각 섹션은 파란 띠에 `STAGE 01 PROFILE` 같은 라벨을 달고 시작한다.

| 스테이지 | 제목 | 안에 있는 것 |
|---|---|---|
| 01 PROFILE | PLAYER 정보 | 소개 문단 2개, 스탯 카드 3장(LEVEL / CLASS / BASE) |
| 02 INTERESTS | POWER-UP 아이템 | 관심사 카드 4장. 테두리 색이 다르고 `+200 PTS` 식 점수가 붙는다 |
| 03 ACTIVITIES | 교내 활동 / 교외 활동 | 두 칸으로 나뉜 목록. 항목마다 연도와 `└─` 부연 설명 |
| 04 CONTACT | SELECT A LINK | `1P` `2P` `3P` 번호가 붙은 링크 3줄 |

- **스탯 카드**와 **관심사 카드**는 화면이 좁아지면 자동으로 한 줄씩 내려간다.
- **관심사 카드** 점수는 200 → 400 → 800 → 1600으로 두 배씩 올라간다. 순서에
  따라 자동으로 붙는 장식이라 관심사를 늘리면 계속 이어진다.
- **활동 항목**은 왼쪽에 세로선이 붙고, 연도는 아케이드 글꼴 노란 글자다.

### 하단 표시줄 (장식)

`© 1998 TAEHEE ARCADE` 와 깜빡이는 블록이 달린 `INSERT COIN`.

### 누를 수 있는 것

누를 수 있는 건 **타이틀 화면의 포트폴리오 버튼 하나**와 CONTACT의 링크
3개(GitHub / LinkedIn / Email)다. 포트폴리오 버튼은 마우스를 올리면 노랑과
검정이 뒤집히고, 링크 줄은 배경이 밝아지며 테두리에 색이 들어온다. 관심사 카드도 배경만
살짝 바뀌지 눌리지는 않는다. 폼, 입력창, 탭 전환은 없다.

## 색

오락실 화면에서 쓰던 원색 계열이다.

| 이름 | 값 | 쓰는 곳 |
|---|---|---|
| 화면 바탕 | `#04040c` | 캐비닛 안쪽 |
| 패널 / 카드 | `#08081a`, `#0d0d22` | 스탯 카드, 관심사 카드, 링크 줄 |
| 프레임 파랑 | `#2121de` | 바깥 테두리, 스테이지 띠 |
| 짙은 파랑 | `#1a1a8f` | 카드 테두리, 활동 세로선 |
| 노랑 | `#ffcc00` | 이름, 섹션 제목, 연도, 팩맨, 1P |
| 하늘 | `#00ffff` | 한 줄 소개, 교내 활동, 2P |
| 분홍 | `#ffb8ff` | 교외 활동, 3P |
| 주황 | `#ffb852` | CREDIT, 스탯 라벨 |
| 빨강 | `#ff0000` | HIGH SCORE, 유령 |
| 글자 | `#e8e8f0` / `#9a9ab8` / `#5c5c8a` | 본문 / 부연 설명 / 하단 표시줄 |

배경이 검정 하나뿐이라 **라이트 모드는 없다.** 시스템 설정과 무관하게 항상
같은 화면이 나온다.

## 글꼴

**픽셀 글꼴 두 종류**를 섞어 쓴다. 글자가 뭉개지지 않도록 안티에일리어싱을 껐다.

- **Press Start 2P** — 영문·숫자 아케이드 문구 전용(이름 로마자, STAGE 라벨,
  점수판, 연도, PTS). 오락실 기판 글꼴 느낌의 대표격이다.
- **DungGeunMo** — 한글 본문 전부. 앞서 쓰던 터미널 글꼴에서 한글만 폭이
  어긋나던 문제가 이걸로 해결됐다.

크기는 이름이 가장 크고 화면 폭에 따라 자동으로 늘었다 줄고, 그다음이 섹션
제목, 본문, 부연 설명, 아케이드 라벨(8~11px) 순이다.

## 움직임

전부 계단식(steps)이라 8비트처럼 뚝뚝 끊긴다.

- 팩맨 입 여닫기
- 유령 위아래 흔들림(셋이 조금씩 시차를 두고)
- 유령 무리가 옆으로 전진
- HIGH SCORE, `▼ SCROLL TO START ▼`, INSERT COIN 블록 깜빡임

페이지 전환 효과나 스크롤 애니메이션은 없다.
